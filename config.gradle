ext {
    def major = 5
    def minor = 4
    def patch = 9

    buildConfig = [
            "minSdk"    : 21,
            "compileSdk": 36,
            "targetSdk" : 33,
            "buildTools": "36.0.0"
    ]
    releaseConfig = [
            "group"      : "com.infinum.dbinspector",
            "version"    : "$major.$minor.$patch",
            "versionCode": major * 100 * 100 + minor * 100 + patch
    ]
}
